///  development or production:
pub const env = .development;

/// log level in increasing order of verbosity:
pub const _log_level = "6";

/// default port for dns server:
pub const _port = "53";

/// the default network interface address to listen on:
pub const _address = "0.0.0.0";

/// where data files should be persisted by default:
pub const _data_dir = "/opt/mns/";